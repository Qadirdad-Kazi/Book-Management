# Backend Environment Variables

# Server Configuration
PORT=5555
NODE_ENV=development # Change to 'production' for production deployment

# MongoDB Configuration
MONGODB_URI=mongodb+srv://<username>:<password>@<cluster>.mongodb.net/bookstore

# JWT Configuration
JWT_SECRET=your_secure_jwt_secret_key
JWT_EXPIRES_IN=1d

# Frontend URLs
FRONTEND_URL=http://localhost:5173 # Local development
# FRONTEND_URL=https://book-management-kazi.netlify.app # Production frontend URL
